name: Build and publish app store

on:
  push:
    branches: [main]
  workflow_dispatch: {}

concurrency:
  group: gh-pages-deploy
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build app store bundle
        # Pinned to a release tag rather than @main: this job has
        # contents:write on this repo, so an unpinned floating branch ref
        # would let a future upstream change run with that permission here
        # without any review on our side. Bump deliberately when needed.
        uses: IceWhaleTech/build-appstore-action@v1.1.2
        with:
          source: .
          output: dist
          base-url: https://nazarborodavka.github.io/owlbear-sync-store/

      - name: Deploy to GitHub Pages (gh-pages branch)
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: gh-pages
          force_orphan: true
